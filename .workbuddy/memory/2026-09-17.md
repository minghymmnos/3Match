
## UI 三问题修复（2026-09-17 05:00）

1. 游戏内背景不平铺根因：CanvasScaler 为 match-height（matchWidthOrHeight=1），画布实际高恒为 2532、宽随屏幕比例变化；FitCover 却按固定宽 1170 反推高度 → 非 1170/2532 比例下上下露出灰底。修复：FitCover 改为 canvasH=DesignHeight、canvasW=DesignHeight*screenAspect，再取 max 缩放（主界面/关卡内两处背景同时受益）。
2. 按钮无文字提示：顶栏齿轮按钮原为固定 x=500 定位（按钮一半悬在顶栏上沿外、不同比例下漂移）。改为右缘锚定（anchor 1,0.5），按钮完整收进顶栏，'设置'/'暂停' 提示文字放按钮左侧（右对齐锚定）。目标栏补文字说明：collect → '收集红花'等（PieceColorExt.Cn），crate → '消除木箱'，ice → '清除冰层'；进度数字与说明上下排布。
3. 关卡选择排版：cellW/cellH 220x210 → 290x230，节点图标放大（IconButton 240x180）；'困难'标签从节点内 (0,-62) 移到节点上方 (0,118) 并加深色描边，不再与数字重叠；锁定节点加'未解锁'提示字；新增地图标题'— 选择关卡 —'（白字深描边，y=452）；网格整体下移 50 避让标题。

## 主界面 UI 二轮修复（2026-09-17 04:50，依据用户截图）

- 根因级 bug：TextButton 文字标签不可见 —— Text() 建的矩形是默认中心锚点，offsetMin(20,10)/offsetMax(-20,-10) 在中心锚点下得到负尺寸矩形（宽 -40）→ '继续关卡/商城/活动'及所有弹窗文字按钮全部空白。修复：先设拉伸锚点 (0,0)-(1,1) 再设内边距。
- 关卡地图容器：弃用被裁切变形的横版 BgMap（Cover 后只剩中部随机花园切片），改用 ui_panel_base_01 九宫格（SpriteLib.PanelSliced，border 90/90/90/230，顶部边框含缎带），标题'选择关卡'（深棕字）放进缎带。
- 节点网格适配面板内容区：cellW 260 x cellH 170、网格 (780,720)@(0,-95)；节点图标 150 方形；困难关改用美术困难节点图 LevelNode(3)（删除文字标签）；锁定提示'未解锁'缩小放锁图标内 (0,-54)。
- 字体协调：标题 120→130 加深色描边；顶栏数字 44→50、恢复倒计时 30→32、图标 72→80，数字加深描边。

## 顶栏/按钮文字微调（2026-09-17 05:05，依据用户截图二）

- TextButton 文字：等边距几何居中下雅黑行盒基线让字看起来偏下 → 改为底边距 18 / 顶边距 4 的光学补偿（整体上提约 7 单位），显式 MiddleCenter。
- 主界面顶栏重排（数字框左缘与图标右缘留 ~10px 间隙）：心图标 -545、生命数 -425（140 宽）；金币图标 -160、金币数 -30（170 宽）；星星图标 135、星星数 265（170 宽）；恢复倒计时随生命数对齐。
- 关卡内目标栏同步微调：图标中心偏移 60→52，进度数字/说明框 x 30→44、宽 -130→-150，消除框重叠。

## 特殊棋子组合技按用户规则校准（2026-09-17 05:15）

- 火箭+炸弹：由 3 行+3 列十字改为只按火箭朝向取 3 行（横向火箭）或 3 列（纵向火箭），comboName 区分'三行横扫/三列贯穿'。
- 彩球+普通：去掉'额外 3 个转随机特殊引爆'，严格为清除全场同色。
- 彩球自身不再参与 ExpandChain：BuildComboPlan 中彩球位置延后到连锁展开后加入 cells，修复彩球+普通/特殊时彩球被当引爆点多清一种最常见颜色的隐藏行为。
- 其余组合已符合规则：火箭+火箭=十字行列，炸弹+炸弹=5x5，彩球+特殊=同色全部转化为该特殊并连锁引爆，彩球+彩球=全盘清除。

## 修复第 9 关冰块数量与目标不符（2026-09-17 05:10）

- 配置错误：levels.json 第 9 关 ices 只配了 5 个冰格（hp2），目标却要求消除 8 个冰块 → 永远无法通关。
- 修复：补充 3 个冰格 (0,3)/(6,3)/(3,6) hp2，现共 8 格与目标一致；布局分散于中下部。
- 全局校验 6-10 关：所有 clear 目标的 count 均与实际障碍格数一致。
- 口径说明：icesDestroyed 按格计数（hp 只影响破坏次数），count 必须等于冰格数。

## 特殊棋子/组合技动画节奏放慢（2026-09-17 05:40）

- 消除爆发 Burst：0.35s→0.55s，淡出改平方曲线（前段停留更久）；新增 BurstDelayed 支持延迟爆发。
- 大面积消除改波浪式展开：ResolvePlan 中按棋子到方案中心的距离延迟 0~0.3s 依次爆开，可看清消除扩散方向。
- 飘字 FloatText：0.9s→1.4s，上升速度 90→55/s，前 70% 时间保持不透明；组合技名称可读完。
- 全屏闪光 Flash 默认 0.25→0.45s（组合技显式传 0.55）；彩虹波 0.45→0.8s。
- 单击激活新演出 ActivateRoutine：特殊棋子先脉冲放大（PulseView，0.45s，放大 1.5 倍回弹）→ 飘字+爆光停留 0.35s → 才开始引爆，明确激活来源。
- ExecutePlan 组合技演出后新增 0.45s 停顿，再进入级联消除。
- 特殊棋子生成 PopScale 0.25→0.45s；ResolvePlan 消除停留 0.12→0.3s。

## 修复 CS0021（2026-09-17 05:45）

- BoardController.ResolvePlan 中 plan.cells[plan.cells.Count/2] 报 CS0021：ClearPlan.cells 是 HashSet<Vector2Int> 不可下标索引。
- 修复：波源中心改为 foreach 累加所有格子的 CellPos 后取平均（效果更稳，且与容器类型解耦）。
- 教训：ClearPlan.cells 是 HashSet，只能 foreach/Contains/Add，不能按索引访问。

## 撰写项目 README（2026-09-17 06:00）

- 参照 LinkGame/README.md 的结构（概述/功能/脚本结构/数据流/关键技术/配置数据），输出 E:/unityhub/project/3Match/README.md。
- 内容覆盖：主界面与关卡内 UI、三消机制、4 特殊棋子（单击激活/朝向）、6 组合技（用户校准规则）、动画节奏表、障碍（木箱/冰层计数口径）、经济存档、13 个脚本职责与关键函数、数据流调用链、关键技术点（场景自举/纯数据层+SyncViewsToModel/交换取视图时序/素材清理管线/波浪演出/分段重力/竖屏适配）、已实现与占位边界、levels.json 字段与 10 关设计表。
